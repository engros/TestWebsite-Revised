<% provide(:title, "Edit user") %>
<% provide(:button_text, 'Save changes') %>  <!-- variable passing for form button on edit page -->
<% provide(:url_action, user_path) %>
<h1>Update your profile</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <a href="http://gravatar.com/emails" target="_blank" rel="noopener noreferrer">change</a>
    </div>
  </div>
</div>

<!--
using <'% ... %> that Rails should call the provide function and associate the string "Home" with the label :title.
we use the closely related notation <'%= ... %> to insert the title into the template using Ruby’s yield function
-->

<!--
target="_blank" is vulnerable to a phishing attack called tabnapping when opening new tab/window,
rel="noopener noreferrer"  fixes this by disallowing control over the window.opener pointing to the window
of the page you are currently viewing such as index.html. Therefore no fake pages can be opened that can steal your info!
-->